# Brainstorming Tree

简体中文 | [English](README.md)

**一个用于持久化脑暴的轻量级 Agent Plugin。** 它把想法保存为可审计的 Idea Tree（创意树），让长时间运行的 Agent 工作能够抵抗上下文遗忘、内容干扰和会话中断。

> **项目状态：** 早期开发阶段（`v0.1.0`）。与具体 Agent 无关的持久化核心已经实现，针对状态转换与协议的检查均已通过。跨 Agent 打包和长期真实脑暴项目验证仍待完成。

## 为什么要做这个项目

一次长时间脑暴会要求语言模型同时记住很多东西：最初目标、不同分支、被否定的假设、已有证据、修改过程，以及一个方案为什么取代另一个方案。随着对话增长，早期信息可能离开当前上下文，也可能被后续无关内容稀释。常见结果是：重复提出旧想法、忘记约束、前后判断不一致，或丢失原本值得继续探索的分支。

Brainstorming Tree 把这部分状态移出对话记忆。对话仍然是交互界面，但项目本地的创意树会成为唯一可信状态来源。

## 核心方案

Agent Plugin 由四个刻意保持简单的部分组成：

1. **可移植的 Agent Skill：** 规定如何选择、扩展、评估和综合想法。
2. **本地 MCP 服务器：** 提供类型明确的创意树操作，避免依赖模型反复改写长篇文字来维持一致性。
3. **SQLite 数据库：** 在当前工作区保存节点、上下级关系、评估、版本和操作历史。
4. **轻量运行时适配层：** 对接不同 Agent Host 各自的清单、配置和安装约定。

```text
用户目标
   │
   ▼
Agent + 可移植脑暴技能
   │  类型明确的操作
   ▼
本地 MCP 服务器
   │  数据库事务
   ▼
<workspace>/.idea-tree/ideas.sqlite3
```

运行时以本地为主，只依赖 Python 标准库。核心循环不需要云端服务、账号、向量模型或第二个 LLM。

## 跨 Agent 兼容边界

可移植核心建立在两个开放接口上：

- [Model Context Protocol（MCP）](https://modelcontextprotocol.io/specification/2025-11-25)：承载类型明确的工具和 Host–Server 通信。
- [Agent Skills](https://agentskills.io/specification)：承载 `SKILL.md` 行为约定。

“Agent Plugin”代表这个项目的产品边界：状态引擎、MCP 工具和脑暴行为不属于某一个 Agent 产品。但这**不代表**所有 Agent 都能用同一个安装包零配置运行。每个 Host 必须支持对应的开放接口，并且可能需要一个很薄的适配层来处理自己的清单、启动命令、权限或安装路径。

当前仓库保留 `.codex-plugin/plugin.json`，把它作为第一个 Codex 适配层。它只是兼容入口，不是项目身份或架构边界。后续 Agent 适配层应保持轻量，并复用同一个 MCP 服务器和 Agent Skill。

## 对 MCTS 的实际改造

Brainstorming Tree 借用了 Monte Carlo Tree Search（蒙特卡洛树搜索，简称 MCTS）中有价值的控制循环，但不会假装随机模拟能够判断一个真实世界想法是否优秀。

| 阶段 | 实际行为 |
| --- | --- |
| **选择（Select）** | 使用置信上界分数，在高价值分支与探索不足的分支之间取得平衡。 |
| **扩展（Expand）** | 添加机制上真正不同的方向、可验证想法或综合方案。 |
| **评估（Evaluate）** | 记录成本最低但有效的判别依据，例如证据、实验、逻辑链或用户的明确判断。 |
| **回传（Backpropagate）** | 把评估统计更新到当前节点及其祖先节点，用于分配下一轮探索注意力。 |

分数只用于引导探索，不代表真相、信心或证明。失败想法会被保留；经过修正的新方案必须作为带有前任关系的新节点加入，而不能悄悄覆盖旧方案。

## 目标能力

- **状态持久化：** 即使上下文被压缩或进程中断，也可以继续之前的讨论。
- **关系明确：** 每个想法都保留父节点、前任方案和综合来源。
- **修改可审计：** 每次变更和评估都会留下操作记录。
- **避免陈旧覆盖：** 通过版本检查，阻止旧上下文覆盖更新后的状态。
- **删除可追溯：** 删除只创建墓碑标记，不物理抹掉历史。
- **数据本地化：** 默认由当前项目工作区持有脑暴数据。

## 当前进度

| 组件 | 状态 |
| --- | --- |
| 插件清单和元数据 | 校验通过 |
| Idea Tree 脑暴技能 | 校验通过 |
| 本地 MCP 服务器 | 核心实现完成 |
| SQLite 数据结构和 13 个创意树操作 | 核心实现完成 |
| CRUD、评估回传、失效处理与协议检查 | 已通过 |
| 第一个运行时适配层 | 已有 Codex 清单 |
| 其他 Agent 适配层和安装说明 | 待完成 |

在跨 Agent 打包和真实长期项目试用完成以前，请把本仓库视为实验性 Agent Plugin，而不是已经达到生产标准的工具。

## 仓库结构

```text
.
├── README.md
├── README_zh.md
├── brainstorming-tree/
│   ├── .codex-plugin/plugin.json     # 第一个运行时适配层：Codex
│   ├── .mcp.json                     # 本地 MCP 服务器声明
│   ├── skills/
│   │   └── idea-tree-brainstorming/  # 可移植 Agent Skill 和状态规则
│   └── scripts/                      # 状态引擎和 MCP 服务器
└── docs/important-information/       # 本地决策与项目状态（Git 已忽略）
```

## 计划中的使用流程

1. 向兼容的 Agent 提供一个问题、决策或开放式想法。
2. 建立一棵具有不可变目标、成功条件和停止条件的创意树。
3. 由插件选择分支、加入不同候选方案，并记录有证据支持的评估。
4. 在之后的会话中继续使用同一棵树，而不需要从聊天记录重新拼接状态。
5. 输出最强的存活综合方案，同时说明决定性证据、未解决分支和重新开启条件。

## 路线图

### `v0.1` — 持久化核心

- [x] 实现不依赖第三方库的本地 MCP 服务器。
- [x] 增加基于 SQLite 的树、节点、评估、选择、历史和快照操作。
- [x] 用最小端到端检查证明状态持久化和祖先评估回传。
- [x] 通过插件清单和技能校验器。
- [x] 检查版本冲突、墓碑删除、评估失效和两种 JSON-RPC 生命周期入口。
- [ ] 写明可移植核心接口，并为受支持的 Agent 运行时打包轻量适配层。

### 后续阶段，仅在真实证据支持时推进

- 根据真实使用记录改进排序和评估标尺。
- 根据真实用户需求增加并验证运行时适配层。
- 增加导入、导出和简洁的人类可读报告。
- 在不削弱本地核心的前提下探索可视化、协作或远端同步。

## 设计原则

- 状态必须存在于模型之外。
- 证据高于表达能力和模型信心。
- 探索分数只分配注意力，不决定真相。
- 反例和失败分支是学习资产，必须保留。
- 可靠的最小机制优先于功能数量。

## 数据与隐私

计划中的数据库位于 `<workspace>/.idea-tree/ideas.sqlite3`，默认会被 Git 忽略。它可能包含敏感脑暴内容；分享、导出或修改忽略规则前，请先检查其中的数据。本地 `docs/` 目录也会被忽略，避免工作记录被意外发布。

## 参与贡献

项目目前处于架构与核心实现阶段。建议优先关注持久化状态边界、确定性的创意树操作、陈旧上下文下的正确性，以及小型端到端示例。在本地核心得到证明以前，请避免引入云端服务或大范围界面功能。

## 开源协议

[MIT](LICENSE) © 2026 Jinwei Lu。
